#! /bin/sh

set -e

if [ "$1" = "configure" ]; then
        if [ -d /usr/doc -a ! -e /usr/doc/liblockfile-dev -a -d /usr/share/doc/liblockfile-dev ]; then
                ln -sf ../share/doc/liblockfile-dev /usr/doc/liblockfile1
        fi
fi

